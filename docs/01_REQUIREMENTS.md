# Requirements Traceability Matrix — PS-26145

## Functional Requirements

| Req ID | Category | Requirement Description | Track | Implementation Component | Status | Validation Evidence |
|---|---|---|---|---|---|---|
| R01 | Telemetry | Ingest passive network telemetry without active packet injection | Infrastructure | Zeek LTS (JSON logs) | Verified | Tested on PCAP replays |
| R02 | Ingest | Asynchronous incremental log parsing | Ingest | `ingest/parser.py` | Verified | `test_parser.py` (7 tests) |
| R03 | Features | Sliding window flow feature extraction | Feature Engine | `features/flow_stats.py` | Verified | `test_flow_stats.py` (16 tests) |
| R04 | Alerts | Standardized unified DRAFT alert contract | Common Alert Layer | `alerts/draft.py`, `docs/04_alert_schema.json` | Verified | `test_alerts.py` (11 tests) |
| R10 | DDoS | Statistical & entropy-based volumetric DDoS detection | Track B (Custom) | `detectors/ddos/` | Verified | `test_ddos_detector.py` (20 tests) |
| R11 | C2 Beaconing | Periodic connection beacon detection | Track A (Reused) | RITA v5.1.2 + `rita_adapter.py` | Verified | Fixed (score 0.977) and ~20% jitter (score 0.916) detected with threshold 0.75; background HTTP/SSL suppressed (<0.635); DNS periodic false positives documented; deduplication verified; `test_rita_beacon_adapter.py` (16 tests pass) |
| R12 | DGA | Machine learning NXDomain / algorithmic domain classifier | Track B (Custom) | `detectors/dga/` | Verified | FANCI-inspired Random Forest (100 trees, 6 features); threshold=0.30; 100% on dga-001, 73% on dga-002, 70.67% unseen Necurs; `test_dga_*.py` pass |
| R13 | DNS Tunnel | High-frequency / payload-encoded DNS query detection | Track A (Reused) | RITA v5.1.2 + `rita_tunnel_adapter.py` | Verified | RITA v5.1.2 C2 Over DNS integration; custom UDP/53531 Zeek analyzer; deduplication state machine; canonical PCAPs evaluated; honest negative analysis documented; `test_rita_tunnel_adapter.py` (7 tests pass) |
| R14 | TLS Anomaly | Encrypted traffic metadata and certificate inspection | Track B (Custom) | `detectors/tls/` | Verified | Dual-signal fusion (JA3/JA3S offline blacklist + statistical behavioral anomaly scoring); zero payload decryption; threshold=0.50; ENC-001 (score 0.0002 -> fingerprint_only) & ENC-002 (score 0.5755 -> both/encrypted_malware); `test_tls_detector.py` (23 tests pass) |
| R15 | Reconnaissance | Port and address scan detection reusing Zeek Scan framework | Track A (Reused) | `detectors/scanning/zeek_scan_adapter.py` | Verified | RECON-001 (1024 ports) & RECON-002 (16 ports) detected; threshold=15; BENIGN-002 clean (0 FP); `test_zeek_scan_adapter.py` (8 tests pass) |
| R16 | Exfiltration | Asymmetric upload/download volume & ratio detection | Track B (Custom) | `detectors/exfiltration/` | Verified | Windowed directional byte accounting (internal source -> external destination); ratio_threshold=10.0, volume_floor=50KB; per-host rolling baseline; asymmetric upload detected (500KB, ratio 500,000:1); inbound download clean (0 alerts); internal iperf3 clean (0 alerts); `test_exfiltration_detector.py` (20 tests pass) |
| R21 | Fusion | OT-aware alert correlation and criticality scoring | Correlation Engine | `fusion/` | Verified | Source-based multi-threat correlation (5-min window, >=2 distinct threat classes); shared UUIDv4 correlation_id; asset criticality lookup (config/assets.yaml); deterministic severity escalation (base + criticality + correlation); bit-for-bit confidence preservation; defense-in-depth schema validation; `test_fusion.py` (14 tests pass) |
| R22 | Storage | Local-first persistent alert storage | Database | SQLite `storage/` | Verified | WAL mode SQLite alert storage (`data/alerts.db`); parameterized queries; schema-compliant alert ingestion; multi-parameter filtering & search; KPI aggregations; correlation incident grouping; `test_storage.py` (8 tests pass) |
| R23 | Visualization | Operator web console and alert drill-down | Presentation | Streamlit `dashboard/` | Verified | Pure presentation console (Blue/Yellow/White SOC hierarchy); KPI cards; compact filter bar; forensic alert inspector with supporting evidence tables; Altair timeline & distribution charts; multi-stage attack chain view; lab asset inventory; `test_dashboard_queries.py` (7 tests pass) |
